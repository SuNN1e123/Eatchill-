# Core dependencies (work everywhere)
streamlit>=1.33.0
Pillow>=9.0.0
numpy>=1.20.0
opencv-python-headless>=4.5.0

# Conditional dependencies
tflite-runtime>=2.5.0,<2.10.0; python_version < '3.12'  # Only for Python 3.9
picamera2; python_version < '3.12' and sys_platform == 'linux'  # Raspberry Pi only

# Mock dependencies (for cloud fallback)
requests>=2.25.0  # For potential API fallback
